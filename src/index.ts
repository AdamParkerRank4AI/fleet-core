/**
 * Convenience re-exports for types. Components are loaded by direct
 * subpath import (`@rank4ai/fleet-core/components/FAQ.astro`) since
 * Astro components don't move through TypeScript barrel files.
 */

export * from "./types/manifest";
